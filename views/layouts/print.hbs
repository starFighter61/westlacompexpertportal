<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Print Invoice</title>
  <!-- Add any print-specific CSS links here if needed -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #fff; /* Ensure white background for printing */
      -webkit-print-color-adjust: exact !important; /* Chrome, Safari */
      color-adjust: exact !important; /* Firefox */
    }
    @media print {
      body {
        margin: 0;
        padding: 1cm; /* Add some padding for printing */
      }
      .no-print {
        display: none !important; /* Hide elements not meant for printing */
      }
      a {
        text-decoration: none;
        color: inherit; /* Avoid blue links on print */
      }
    }
  </style>
</head>
<body>
  <div class="container">
    {{{body}}}
  </div>
</body>
</html>